# Fast profile — target ≥60 FPS detect+track on RTX 4060 CUDA
weights: weights/best.pt
imgsz: 512
conf: 0.30
iou: 0.45
device: auto
half: true
tracker: sort
sort:
  max_age: 30
  min_hits: 3
  iou_threshold: 0.3
