# Fast profile — speed / default track (lower conf + coast for hit-rate)
weights: weights/best.pt
imgsz: 512
conf: 0.25
iou: 0.45
device: auto
half: true
tracker: sort
sort:
  max_age: 30
  min_hits: 3
  iou_threshold: 0.3
  coast_frames: 25
